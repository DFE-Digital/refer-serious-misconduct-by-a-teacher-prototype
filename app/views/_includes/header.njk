{% if data.report.email %}
  {% set serviceUrl = '/report' %}
{% else %}
  {% set serviceUrl = '/' %}
{% endif %}


{{ govukHeader({
    homepageUrl: "#",
    serviceName: serviceName,
    serviceUrl: serviceUrl,
    navigation: [
      {
        href: "/report",
        text: "Your referrals"
      } if data.report.sentDate,
      {
        href: "/account/sign-out",
        text: "Sign out"
      }
    ] if data.report.email
  }) }}